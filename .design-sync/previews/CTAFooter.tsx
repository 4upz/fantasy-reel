import { CTAFooter } from 'fantasy-reel'

/**
 * The closing call-to-action band on the marketing page. It takes no props —
 * headline, subhead and button copy are fixed, so this is the whole component.
 */
export const Default = () => <CTAFooter />
