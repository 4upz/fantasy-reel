import { ScoringReveal } from 'fantasy-reel'

/**
 * Explains the scoring curve by contrasting a critical hit against a flop —
 * the same baseline-relative maths TomatometerScore encodes. Takes no props;
 * the two examples are fixed in the component.
 */
export const Default = () => <ScoringReveal />
