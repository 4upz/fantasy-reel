---
category: League
---

# DraftBoardHeader

Presentational draft status card: active team, round and pick, progress ring,
and an optional queue slot. Supply resolved turn data; the component does not
advance the draft. `isMyTurn` uses the app's turn highlight and
`isDraftComplete` displays finalization status. Keep the default responsive
layout; `wide` is reserved for fixed-canvas examples.
