---
category: Movies
---

# RosterPoster

Roster artwork with the shared TMDb URL resolution and a film-icon fallback.
Mount it in a relatively positioned container with explicit dimensions or
an aspect ratio: the image fills its parent. `src` can override the stored
poster path and `sizes` describes the rendered width.
